# Use the official Nginx image as the base image.
# Nginx is a lightweight web server that can serve static HTML files.
FROM nginx:latest

# Copy our custom index.html from the current project folder
# into Nginx's default website directory inside the image.
COPY index.html /usr/share/nginx/html/index.html